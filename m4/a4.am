
a4includedir=${includedir}/a4
a4pythondir=${pythondir}/a4
a4pythonprotodir=${pythondir}/a4/proto

edit = sed -e 's|$${prefix}|$(prefix)|g'
 
this_a4.sh: this_a4.sh.in
	rm -f $@ $@.tmp
	srcdir=''; \
	test -f ./$@.in || srcdir=$(srcdir)/; \
	$(edit) $${srcdir}$@.in > $@

install-exec-hook:
	cd $(DESTDIR)$(bindir) && \
	if ! test -e python; then $(LN_S) `which $(PYTHON)` python; fi

bin_SCRIPTS = this_a4.sh
EXTRA_DIST = this_a4.sh.in
CLEANFILES = this_a4.sh
