#!/usr/bin/env python
from sys import argv, exit
from a4 import A4ReaderStream
if len(argv) < 2:
    print "Usage: %s <files>" % argv[0]
    exit(-1)

for fn in argv[1:]:
    info = A4ReaderStream(open(fn)).info()
    print("%s: %s" % (fn, info))


